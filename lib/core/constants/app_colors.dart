import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const Color background = Color(0xFFFAF9F8);
  static const Color white = Color(0xFFFFFFFF);

  // Primary Button
  static const Color primaryButton = Color(0xFF68B0AB);

  // Text
  static const Color textPrimary = Color(0xFF181619);
  static const Color textSecondary = Color(0xFF1A1A1A);
  static const Color textOnButton = Color(0xFF090814);

  // Indicators
  static const Color indicatorActive = Color(0xFF4A7C59);
  static const Color indicatorInactive = Color(0x66000000); // rgba(0,0,0,0.4)

  // Shadow
  static const Color shadow = Color(0x40000000); // rgba(0,0,0,0.25)
}
